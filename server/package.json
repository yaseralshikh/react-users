{
  "name": "server",
  "version": "1.0.0",
  "description": "Node.js + TypeScript + Express + Prisma + Inngest back-end for the User Management project",
  "main": "dist/index.js",
  "scripts": {
    "dev": "ts-node-dev --respawn --transpile-only src/index.ts",
    "build": "tsc",
    "start": "node dist/index.js",
    "prisma:migrate": "prisma migrate dev",
    "prisma:generate": "prisma generate",
    "test": "echo \"No tests yet\""
  },
  "dependencies": {
    "@prisma/client": "^6.15.0",
    "cors": "*",
    "express": "*"
  },
  "devDependencies": {
    "@types/cors": "*",
    "@types/express": "*",
    "@types/node": "*",
    "prisma": "^6.15.0",
    "ts-node-dev": "*",
    "typescript": "*"
  }
}
